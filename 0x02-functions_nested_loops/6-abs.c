#include "main.h"

/**
 * _abs - compute the absoulte value of an integer
 * @n: the integer to be computed
 *
 * Return: teh absoulte value of the integer
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
