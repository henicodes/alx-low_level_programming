#include "main.h"

/**
 * main - test the funcation that prints if integer is postive or negative            
 * Return: zero
 */                                                                            
int main(void)
{
int i;
i = 0;
positive_or_negative(i);
return (0);
}
